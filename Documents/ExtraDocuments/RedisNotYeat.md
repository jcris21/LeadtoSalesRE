Con el volumen del MVP (~9,000 leads/mes, ~0.4 msg/seg en pico) y las decisiones ya tomadas a lo largo de las 9 iteraciones, mi recomendación es no agregar Redis ahora. Razonemos por los usos típicos que tendría en un Coordinator SAS como el que diseñamos, y por qué cada uno ya está resuelto o no se justifica a este volumen:
Uso típico de Redis en un agente SAS¿Ya resuelto en este diseño?¿Amerita Redis ahora?Checkpointing de LangGraph (persistir el estado del grafo entre turnos)Sí — decidido en Iteración 2: el estado vive en Postgres (ConversationStateMachinePort), no en memoria del proceso. LangGraph soporta un PostgresSaver nativoNo — agregar un RedisSaver duplicaría el mismo propósito que Postgres ya cumple, sin necesidadCorrelación de respuesta asíncrona (ack inmediato → respuesta que llega después, §7.4)Sí — resuelto con el patrón Outbox/Inbox sobre Postgres (Iteración 1)No — Redis pub/sub sería más rápido en microsegundos, pero a 0.4 msg/seg esa diferencia es irrelevanteLock distribuido para el Outbox Worker (single-writer)Explícitamente diferido en Iteración 9 — un solo worker con auto-restart alcanza a este volumenNo — es el caso clásico donde Redis (Redlock) brillaría, pero el disparador de activación (contención entre múltiples workers) no existe todavíaCache de lecturas frecuentes (OrganizationConfig, PromptVersion activo)Parcialmente — hoy se lee de Postgres en cada turnoMarginal — a este volumen, una lectura indexada de Postgres es sub-milisegundo; el cache ahorraría una consulta que ya es barataRate limiting / control de costo por organización (Gate 4 de la skill agentic: costo marginal)No implementado todavíaAún no — sería el caso de uso más defendible de Redis, pero a 9,000 leads/mes el costo total de LLM es predecible sin necesitar un contador de alta frecuenciaVentana de contexto conversacional (últimos N mensajes + perfil resumido, Gate 7)Resuelto vía MessageRef en PostgresNo — el volumen no genera presión de lectura que justifique una capa de cache adicional
El patrón de decisión es el mismo que aplicamos en la Iteración 9: cada rol que Redis podría cumplir ya tiene un mecanismo Postgres-based que lo resuelve a este volumen, y agregar una segunda tecnología de almacenamiento sin una necesidad medible es exactamente el tipo de "deuda por adelantado" que evitamos con multi-AZ y la réplica de lectura — más superficie operativa para un equipo pequeño (CON-7), sin beneficio de latencia perceptible.
Disparadores de activación concretos (mismo formato que usamos para diferir conceptos en §5.1):
CasoDisparador para introducir RedisLock distribuido del Outbox WorkerSe necesitan múltiples workers activos por contención real de eventos (checkpoint x10)Rate limiting / control de costo LLMEl costo por organización se vuelve impredecible o se necesita throttling en tiempo real por agenciaCache de configuraciónUn profiling real muestra que get_active_prompt()/get_config() es un cuello de botella medible bajo cargaSemantic cache de respuestas LLMEl costo de LLM por conversación se vuelve significativo y hay alta repetición de preguntas (FAQs del anuncio, objeciones comunes) — este es probablemente el caso más interesante a mediano plazo, no por infraestructura sino por ahorro de costo/latencia en respuestas repetidas